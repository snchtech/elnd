Якщо **ви самостійно будете розробляти веб-інтерфейс**, тоді роль підрядника звужується до **створення бекенду**: API + ШІ-модуль + база знань (RAG) + перевірка орфографії.

У такому випадку в листі варто чітко вказати, що від них очікується **реалізація серверної частини з API**, до якої ваш фронтенд буде звертатися.

---

## 🔧 Функціонал, який має надати підрядник (у вигляді API / бекенду)

### 1. **API для перевірки документів на відповідність**
```http
POST /api/analyze/legal
```
**Вхід:**  
- документ (файл або текст),
- тип перевірки (`law`, `internal_policy`),
- мова (`uk`, `en`).

**Вихід:**
- масив фрагментів з:
  - проблемним текстом,
  - поясненням,
  - посиланням на нормативний документ (назва, номер, пункт),
  - рейтингом впевненості (confidence score).

---

### 2. **API для перевірки орфографії / граматики / стилю**
```http
POST /api/analyze/spellcheck
```
**Вхід:**
- текст (рядок або документ),
- мова.

**Вихід:**
- масив помилок з:
  - позицією помилки,
  - типом (орфографія / граматика / стиль),
  - запропонованими варіантами,
  - коротким поясненням (якщо можливо).

---

### 3. **API для завантаження / оновлення нормативної бази**
```http
POST /api/knowledge/upload
```
**Вхід:**
- файл (PDF, DOCX),
- метадані (назва документа, тип, дата, мітки),
- мова.

**Призначення:**
- Індексація та включення документа до бази знань.

---

### 4. **API для пошуку по базі знань (RAG)**
```http
POST /api/knowledge/search
```
**Вхід:**
- запит (рядок),
- мова.

**Вихід:**
- релевантні фрагменти документів (для реалізації пошуку / пояснень у веб-інтерфейсі вручну).

---

### 5. **API для генерації PDF/звіту з підказками (опційно)**
```http
POST /api/export/report
```
**Вхід:**
- оригінальний текст + результати аналізу.

**Вихід:**
- файл з коментарями (PDF або DOCX), підсвіченими проблемними місцями.

---

## 📦 Формат API (бажано):
- REST або GraphQL;
- JSON як основний формат відповіді;
- Авторизація (JWT / токен).

---

## 🧩 Додаткові функції, які має реалізувати підрядник:
| Функція                           | Пояснення                                                                 |
|----------------------------------|---------------------------------------------------------------------------|
| Індексація документів            | Автоматичне розбиття документів на чанки, побудова ембеддингів.          |
| Векторна база знань (RAG)        | FAISS, Qdrant або Weaviate локально.                                      |
| LLM для аналізу тексту           | Модель, здатна інтерпретувати результат пошуку + генерувати пояснення.   |
| Орфографічний модуль             | Може бути окремим класичним або на базі LLM.                              |
| Підтримка кількох мов            | Українська / Англійська — мінімум.                                        |
| Логування / аналітика            | Опційно — для аудиту запитів, оцінки точності тощо.                      |

---

## 🔒 Безпека та розгортання
- Всі моделі й дані — **локально**, без доступу до хмар.
- API — **локальний сервер** (можливо через Docker).
- Всі документи повинні **оброблятись локально**, не залишаючи меж серверу.

---

Хочеш — можу сформувати окремий додаток до листа із **технічним завданням для API**, який ви можете прикласти до основного листа для підрядника.