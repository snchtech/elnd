Для налагодження React-додатків у браузері вам потрібен плагін **React Developer Tools**. Це офіційне розширення від команди React, яке дозволяє вивчати структуру компонентів, їх стан (state) та пропси (props) у вашому React-додатку.

---

### **Встановлення React Developer Tools**

#### **1. Google Chrome**
- Перейдіть за посиланням: [React Developer Tools для Chrome](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi).
- Натисніть **"Add to Chrome" (Додати до Chrome)**.
- Після встановлення перезавантажте браузер.

#### **2. Mozilla Firefox**
- Перейдіть за посиланням: [React Developer Tools для Firefox](https://addons.mozilla.org/en-US/firefox/addon/react-devtools/).
- Натисніть **"Add to Firefox" (Додати до Firefox)**.
- Перезавантажте браузер після встановлення.

#### **3. Microsoft Edge**
- Перейдіть за посиланням: [React Developer Tools для Edge](https://microsoftedge.microsoft.com/addons/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi).
- Натисніть **"Get" (Отримати)**.
- Перезавантажте браузер після встановлення.

---

### **Використання React Developer Tools**

1. **Відкрийте інструменти розробника браузера:**
   - На Windows/Linux: `Ctrl + Shift + I`.
   - На macOS: `Cmd + Option + I`.

2. **Перейдіть на вкладку `Components`:**
   - Після встановлення плагіну в інструментах розробника з'являться дві нові вкладки:
     - **Components**: Для перегляду структури компонентів, їх state і props.
     - **Profiler**: Для аналізу продуктивності вашого React-додатка.

3. **Виберіть компонент:**
   - У вкладці **Components** можна натиснути на будь-який компонент, щоб побачити його стан (state), властивості (props) та інші дані.

---

### **Що можна робити з React Developer Tools?**

1. **Перегляд дерева компонентів:**
   - Досліджуйте структуру компонентів вашого додатка.
   - Відображається як ієрархічне дерево.

2. **Редагування props і state:**
   - Ви можете вносити зміни в props і state компонентів прямо у вкладці **Components**.

3. **Пошук компонентів:**
   - Використовуйте поле пошуку для швидкого знаходження конкретного компонента за назвою.

4. **Аналіз продуктивності:**
   - Вкладка **Profiler** допомагає зрозуміти, які компоненти перерендерюються і скільки часу це займає.

5. **Взаємодія з контекстом:**
   - Перегляд значень контексту (React Context API) для компонентів, які його використовують.

---

### **Корисні поради**

- **Увімкнення Strict Mode:**
  Якщо ви використовуєте Strict Mode, React Developer Tools може виділити потенційні проблеми у вашому коді.

- **Налагодження у виробничому режимі:**
  Плагін працює у виробничому режимі, але надає більше інформації у режимі розробки.

---

Якщо потрібна допомога з налагодженням React-додатка, звертайтеся! 😊