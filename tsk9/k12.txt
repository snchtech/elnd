Дуже правильне практичне питання! 🔥  
Давай поясню тобі чітко, **як в APEX правильно налаштувати порядок стовпців в Interactive Grid (IG)**, щоб він був стабільним і **не збивався**.

---

# 📋 Чому зараз у тебе порядок стовпців збивається:

| Причина | Пояснення |
|---------|-----------|
| Переміщення вручну на фронтенді | Це тимчасово — тільки для поточної сесії |
| Немає збереженого серверного налаштування | APEX при перезавантаженні або при новому вході будує IG заново за початковим порядком |
| Немає встановленого `Saved Report` або фіксованого порядку в Attributes | Тому повертається до дефолту |

---

# ✅ Як правильно налаштувати порядок стовпців в IG в APEX:

## 1️⃣ Відкрити сторінку в режимі редагування (Page Designer)

- Знайти свій **Interactive Grid** у розділі **Regions**.
- Клікнути на саму область IG.

---

## 2️⃣ Перейти в секцію **Attributes → Columns**

Ти побачиш список усіх стовпців.

---

## 3️⃣ Перетягни мишкою стовпці у потрібному порядку

- Просто перетягни назви полів у **списку Columns** у потрібному порядку.  
- Порядок стовпців у цій панелі **визначає серверний порядок**, який буде завжди при кожному відкритті сторінки.

✅ Саме цей порядок збережеться при відкритті сторінки і для всіх користувачів.

---

## 4️⃣ Додатково:
Якщо у тебе увімкнене `Saved Reports` або `Default Report Settings` для IG, то:

- Переконайся, що **Primary Report** збережений із правильним порядком стовпців.
- Інакше користувачі можуть бачити свій власний раніше збережений порядок.
  
### Як це перевірити:
- В Attributes → Section `Saved Report Settings`
- Перевір, чи стоїть галочка на "Fixed Default Report" (`Only Allow Current Report`)

✅ Якщо хочеш, щоб користувачі **не могли змінювати** порядок — вкажи "Fixed Default Report = Yes".

---

# 📈 Коротко покроково:

| Крок | Дія |
|-----|-----|
| 1 | Перейти в Page Designer |
| 2 | Відкрити Attributes → Columns |
| 3 | Перетягнути стовпці у потрібному порядку |
| 4 | Зберегти |
| 5 | Перевірити налаштування `Fixed Default Report` (якщо потрібно) |

---

# 🛠 Якщо ти хочеш дозволити користувачам змінювати порядок самостійно і зберігати — потрібно використовувати **Saved Interactive Grid Settings** для юзерів. (але це окрема велика тема — можу пояснити якщо треба)

---

# 🚀 Хочеш я ще покажу:

- Як примусово зробити **перезбереження стандартного порядку колонок** через SQL Script або через APEX Admin?
- Або як налаштувати, щоб при першому заході користувач бачив правильний порядок колонок без ручного пересортування?

Напиши "так" ✅  
(І я одразу додам варіанти для адміністрування Saved Reports 🔥)