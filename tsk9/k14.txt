Дуже чітке завдання, розберімо все по кроках! 🔥  
(Ти правильно мислиш: потрібно керувати відображенням `P6_PARENT_NAME` залежно від режиму і наявності parent_id.)

---

# 📋 Завдання:

| Умова | Що має відбуватись |
|------|--------------------|
| Створення нового підзавдання | ❌ `P6_PARENT_NAME` приховано |
| Редагування підзавдання **без** батьківського (`parent_id IS NULL`) | ❌ `P6_PARENT_NAME` приховано |
| Редагування підзавдання **з** батьківським (`parent_id IS NOT NULL`) | ✅ `P6_PARENT_NAME` показано |

---

# 📈 Що ти маєш на сторінці:

- Item: `P6_PARENT_NAME`
- Item: `P6_PARENT_ID`
- Item: `P6_ID` (ідентифікатор підзавдання, якщо ми в режимі редагування)

---

# ✅ Як правильно налаштувати в APEX:

## 1️⃣ Установити `Server-side Condition` на `P6_PARENT_NAME`

Перейди в **P6_PARENT_NAME → Behavior → Server-side Condition**:

| Поле | Значення |
|------|----------|
| Condition Type | PL/SQL Expression |
| PL/SQL Expression | `:P6_ID IS NOT NULL AND :P6_PARENT_ID IS NOT NULL` |
| Condition = True | Show Item |

---

# 🛠 Що означає:

- Якщо `P6_ID IS NOT NULL` ➔ ми в режимі редагування (а не створення)
- Якщо `P6_PARENT_ID IS NOT NULL` ➔ це дочірнє підзавдання
- ➔ Показуємо поле `P6_PARENT_NAME`

---

# 📋 Альтернативний варіант через "Value of Item/Column":

- Condition Type: **Value of Item/Column = Expression**
- Expression 1: `P6_ID`
- Expression 2: `NOT NULL`
- **AND**
- Expression 1: `P6_PARENT_ID`
- Expression 2: `NOT NULL`

Тоді працюватиме аналогічно.

---

# 📈 Як це виглядатиме:

| Сценарій | Видимість `P6_PARENT_NAME` |
|----------|-----------------------------|
| Створення нового підзавдання | ❌ приховано |
| Редагування підзавдання без батьківського | ❌ приховано |
| Редагування підзавдання з батьком | ✅ видно поле (наприклад, "Батьківське завдання: ...")

---

# 🚀 Якщо хочеш:
Можу також запропонувати **красиве оформлення** для цього поля при перегляді, наприклад стилізувати як інформативний лейбл (label-only режим) без можливості редагування.

Написати як це зробити? 🔥  
(Щоб P6_PARENT_NAME виглядав як красивий підпис без поля вводу) ✅
